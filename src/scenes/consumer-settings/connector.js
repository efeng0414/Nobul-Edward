import { connect } from "react-redux";
import ConsumerSettings from "./container";

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ConsumerSettings);
